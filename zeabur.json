{
  "$schema": "https://zeabur.com/schema.json",
  "name": "brightdata-mcp",
  "description": "Custom Bright Data MCP server with 9 tools (search, scrape, discover). All consume the 5,000 free credits / month pool — no paid add-ons. Exposed over HTTP for remote MCP clients.",
  "template": "docker",
  "spec": {
    "port": 8080,
    "env": {
      "BRIGHTDATA_API_KEY": {
        "default": "",
        "required": true
      },
      "BRIGHTDATA_API_TOKEN": {
        "default": "",
        "required": false
      },
      "SERP_ZONE": {
        "default": "serp_api",
        "required": true
      },
      "WEB_UNLOCKER_ZONE": {
        "default": "mcp_unlocker",
        "required": true
      },
      "MCP_TRANSPORT": {
        "default": "http",
        "required": false
      },
      "MCP_HOST": {
        "default": "0.0.0.0",
        "required": false
      },
      "MCP_PORT": {
        "default": "8080",
        "required": false
      },
      "MCP_PATH": {
        "default": "/mcp",
        "required": false
      }
    }
  }
}
