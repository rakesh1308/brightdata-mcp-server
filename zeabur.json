{
  "$schema": "https://zeabur.com/schema.json",
  "name": "brightdata-mcp",
  "description": "Custom Bright Data MCP server with 9 tools for search, page scraping, Web Scraper datasets, and account-gated Discover. Exposed over Streamable HTTP.",
  "template": "docker",
  "spec": {
    "port": 8080,
    "env": {
      "BRIGHTDATA_API_KEY": {
        "default": "",
        "required": true
      },
      "BRIGHTDATA_API_TOKEN": {
        "default": "",
        "required": false
      },
      "SERP_ZONE": {
        "default": "serp_api",
        "required": true
      },
      "WEB_UNLOCKER_ZONE": {
        "default": "mcp_unlocker",
        "required": true
      },
      "MCP_TRANSPORT": {
        "default": "http",
        "required": false
      },
      "MCP_HOST": {
        "default": "0.0.0.0",
        "required": false
      },
      "MCP_PORT": {
        "default": "8080",
        "required": false
      },
      "MCP_PATH": {
        "default": "/mcp",
        "required": false
      }
    }
  }
}
